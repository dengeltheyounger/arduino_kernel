#include "memory.h"

int memory_write(int task_id,
		enum memory_space zone,
		char *buffer,
		int n_bytes) {
	
	return 0;
}

int memory_read(int task_id,
		enum memory_space zone,
		char *buffer,
		int n_bytes) {

	return 0;
}
